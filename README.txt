This is a bare-bone implementation for a cashregister that supports
SEQR payments.

To run the sample, you need JRE6 and maven installed. Running the following
command from command-line should run the sample:

mvn exec:java -Dexec.mainClass="com.seqr.cashregister.sample.Main"
