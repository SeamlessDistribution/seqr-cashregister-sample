This is a bare-bone implementation for a cashregister that supports
SEQR payments.

To run the sample, you need JDK and maven installed. Running the following
command from command-line should run the sample:

mvn package exec:java -Dexec.mainClass="com.seqr.cashregister.sample.Main"
